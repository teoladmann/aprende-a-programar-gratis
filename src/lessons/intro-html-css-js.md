---
title: 'Introducción a HTML, CSS y JavaScript (🏡)'
author: 'Teo'
category: '1 - Introducción'
tags: 'Lenguajes,Casa'
order: 2
---

Para entender qué hace cada uno de estos lenguajes vamos a utilizar la metáfora de construir una casa.

### HTML (Materiales de Construcción)

Si queremos construir una casa lo primero que necesitamos son los materiales de construcción: madera, ladrillos, ventanas, techo, etc. En nuestra metáfora estos elementos que nos permiten armar la estructura de la casa son el HTML (lo veremos más adelante pero las siglas HTML corresponden a _Hypertext Markup Language_). Quizás es más útil por el momento pensar HTML como Hormigón, Techo, Madera y Ladrillos (elementos para armar una casa). Uno puede vivir tranquilamente en una casa hecha de estos elementos como uno también puede armar un sitio web que sea solo de HTML. Pero sería un sitio web muy básico, en blanco y negro y sin ningún tipo de estilo o interactividad.

### CSS (Estilo)

Si quisiéramos darle estilo a nuestra casa necesitaríamos una serie de reglas que determinen que una cierta pared la queremos de color azul, que queremos que el sillón se ubique en un cierto lugar, que la ventana la queremos redonda, etc. Todas estas definiciones se hacen con CSS (lo veremos más adelante pero las siglas CSS corresponden a _Cascading Style Sheets_). En un sitio web, lo que hace el CSS es definir reglas para los elementos de construcción que cumplen con una cierta condición. Por ejemplo, puede definir una regla para que todos los párrafos tengan un cierto tamaño, otra regla para que los títulos sean de un cierto color y muchas (muchísimas) cosas más.

### JavaScript: (Interacción)

Bien, a esta altura tenemos nuestra hermosa casa ya pintada. Lo siguiente que queremos hacer es equiparla. Queremos sumar un televisor, un aire acondicionado, queremos instalarle las luces, el termostato, etc. Básicamente le estamos agregando un cierto comportamiento a la casa y eso se logra agregando programación con JavaScript. En el sitio web, esto equivale a mostrar un mensaje si el usuario hace click en un botón, cambiar una imagen cada unos segundos, etc. De estas cosas se encarga JavaScript. Este tipo de acciones que ocurren en el navegador web (como Google Chrome, Internet Explorer, Firefox, etc) se dice que son del _frontend_ o del _lado del cliente_.

Por otro lado, ¿qué pasa si queremos conectarnos con el mundo exterior? ¿Qué pasa si por ejemplo queremos pedir un delivery de pizza? En ese caso deberíamos encontrar una forma de conectarnos con la pizzería para hacer el pedido. En el mundo web, la pizzería sería lo que se llama un servidor web. Las casas llaman a la pizzería a hacer un pedido igual que los sitios webs llaman a los servidores para hacer un pedido de información. Cuando entramos por ejemplo a **facebook** el sitio web hace un llamado al servidor para que le traiga toda la información del usuario. Al servidor se le dice que es del _backend_. Mientras que el _frontend_ solo se puede hacer con el lenguaje JavaScript, el _backend_ también puede hacerse con JavaScript pero también con otros lenguajes como por ejemplo Python, Ruby, Java, etc.

### Resumen

1. El HTML te da la estructura de un sitio web. Puedo armar un sitio web que tenga solo HTML pero se vería bastante aburrido.
2. El CSS determina a través de una serie de reglas cómo queremos que luzcan los elementos de HTML.
3. Del lado del _frontend_ JavaScript se encarga de hacer que el sitio se vuelva "inteligente" y pueda reaccionar a las interacciones del usuario.
4. Del lado del _backend_ JavaScript (y otros lenguajes) responden a los llamados de los sitios webs pidiendo información a través de un servidor.
